CC = @CC@
LINK = @LINK@
PERL = @PERL@
EXEC = @EXEC@
OBJ = @OBJ@

.PHONY: all clean

all: pm$(EXEC)

#<DEPRULE> ../src

pm$(EXEC): $(OBJS) ./lib/libpm.a
	$(LINK) $@ $(OBJS) ./lib/libpm.a

clean:
	rm -rf *$(OBJ) pm$(EXEC)
